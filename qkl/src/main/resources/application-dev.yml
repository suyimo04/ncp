server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_trace?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: suyimo.04
    driver-class-name: com.mysql.cj.jdbc.Driver

# 区块链配置（dev环境覆盖）
# 开发时默认用模拟模式，连接真实链时改为 false 并填入合约地址和私钥
blockchain:
  mock-mode: false
  # contract-address: "0x部署后的合约地址"
  # private-key: "部署合约时使用的账户私钥"

